
#!/usr/bin/env bash
# This script lists non-hidden entries and prints only the part after the first dash if present
for file in *
do
  [ -e "$file" ] || continue
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  else
    echo "$file"
  fi
done
